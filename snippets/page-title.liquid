<div class="container mx-auto py-12 max-w-7xl text-center">
  <h1 class="font-semibold text-4xl text-center">{{ title }}</h1>

  <div>
    {% if show_breadcrumb %}
      {% render 'breadcrumb' %}
    {% endif %}
  </div>
</div>
